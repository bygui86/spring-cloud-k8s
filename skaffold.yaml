apiVersion: skaffold/v1alpha2
kind: Config
build:
  tagPolicy:
    envTemplate:
      template: "{{.DOCKER_REGISTRY}}/bygui86/spring-cloud-k8s-config:{{.VERSION}}"
  artifacts:
  - imageName: changeme
    workspace: .
    docker: {}
  local: {}
deploy:
  kubectl:
    manifests:
profiles:
- name: dev
  build:
    tagPolicy:
      envTemplate:
        template: "{{.DOCKER_REGISTRY}}/bygui86/spring-cloud-k8s-config:{{.DIGEST_HEX}}"
    artifacts:
    - docker: {}
    local: {}
  deploy:
    helm:
      releases:
      - name: spring-cloud-k8s-config
        chartPath: charts/spring-cloud-k8s-config
        setValueTemplates:
          image.repository: "{{.DOCKER_REGISTRY}}/bygui86/spring-cloud-k8s-config"
          image.tag: "{{.TAG}}"
